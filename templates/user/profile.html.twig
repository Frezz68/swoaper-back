{% extends 'base.html.twig' %}

{% block title %}Swoaper - login{% endblock %}

{% block body %}
    <div class="h-full w-full bg-white flex flex-col justify-center items-center">
        <div class="h-100 flex flex-nowrap flex-col w-100 justify-center items-center">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <p class="mb-4">Email:</p>
                <span> {{ app.user.email }}</span>

                <a href="{{ path('app_logout_page') }}" class="font-medium text-white bg-dark-blue px-3 py-2 drop-shadow-lg color-white rounded-lg">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_login_page') }}" class="font-medium text-white bg-dark-blue px-3 py-2 drop-shadow-lg color-white rounded-lg">Se connecter</a>
            {% endif %}
        </div>
    </div>
{% endblock %}
